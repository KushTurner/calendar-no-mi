meta {
  name: Create Event - Empty Text (expect 400)
  type: http
  seq: 7
}

post {
  url: {{BASE_URL}}/event
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

body:json {
  {
    "text": ""
  }
}

tests {
  res.status == 400
}
