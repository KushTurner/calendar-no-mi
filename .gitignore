# OAuth credentials and user data — never commit these
credentials.json
users.json

# Local environment overrides
.env

# Bruno secret environment values (contains bearer token)
bruno/environments/*.secrets.json
.DS_store
